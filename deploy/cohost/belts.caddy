{$BELTS_SERVER_NAME:belts.example.com} {
	encode zstd gzip
	root * /belts
	try_files {path} /index.html
	file_server

	header {
		X-Content-Type-Options nosniff
		Referrer-Policy strict-origin-when-cross-origin
		Permissions-Policy "browsing-topics=()"
	}
}
